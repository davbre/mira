<% if @key.errors.any? %>
  <div id="error_explanation">
    <div class="alert alert-danger">
      There is <%= pluralize(@key.errors.count, "error") %>.
    </div>
    <ul>
    <% @key.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>
