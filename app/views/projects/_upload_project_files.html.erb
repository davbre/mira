<div class="row">
  <div class="col-md-6 col-md-offset-3">


      <%= form_for(@project, url: {action: "upload_datapackage"} , method: "post", :html => { multipart: true }) do |f| %>

      <div class="control-group">
        <%= f.label :datapackage, :class => 'control-label' %>
        <div class="controls">
        <%= file_field_tag "datapackage", type: :file %>
        </div>
      </div>

      <div class="form-actions">
        <%= f.submit "Upload datapackage.json", :class => 'btn btn-primary', id: 'submit_files' %>
        <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                    projects_path, :class => 'btn btn-mini' %>
      </div>

    <% end %>

  </div>
</div>
