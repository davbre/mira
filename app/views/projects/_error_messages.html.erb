<% if @project.errors.any? %>
  <div id="error_explanation">
    <div class="alert alert-danger">
      There is <%= pluralize(@project.errors.count, "error") %>.
    </div>
    <ul>
    <% @project.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>
